import { Inter, Bebas_Neue, Amaranth } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const bebas = Bebas_Neue({
  subsets: ["latin"],
  weight: ["400"],
});

export const amaranth = Amaranth({
  subsets: ["latin"],
  weight: ["400"],
});
